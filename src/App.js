import React from 'react';
import './App.css';
import FirebaseUI from './FirebaseUI'; // Ajuste o caminho se necessário

function App() {
  return (
    <div className="App">
      <header className="App-header">
        {/* Substitua o conteúdo que deseja por FirebaseUI */}
        <FirebaseUI />
      </header>
    </div>
  );
}

export default App;
